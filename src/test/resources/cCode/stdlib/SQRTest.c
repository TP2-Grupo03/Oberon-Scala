#include <stdio.h>
#include <math.h>

int main() {
    double x;
    double y;
    double z;
    double w;

    w = 196.0;
    x = 10.0;


    y = x * x;  
    z = w * w;  

    printf("y (SQR(x)): %f\n", y);
    printf("z (SQR(w)): %f\n", z);

    return 0;
}
